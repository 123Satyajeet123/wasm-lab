# Build output
target/
node_modules/
__pycache__/
.venv/

# Vendored upstream checkouts — cloned by setup, not committed.
# blitz is ~2 GB with its target dir; WPT is 1.1 GB / 163k files.
upstream/

# Render output and diffs, regenerated by compare.py
out/

# Fonts are NOT redistributable — the measurements used the macOS system Arial.
# Run ./setup-fonts.sh to fetch openly-licensed faces instead. See README.
*.ttf

.DS_Store
